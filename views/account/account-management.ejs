<% if (flashMessages.success) { %>
<div class="flash-success"><%= flashMessages.success %></div>
<% } %> <% if (flashMessages.error) { %>
<div class="flash-error"><%= flashMessages.error %></div>
<% } %>

<h1><%- title %></h1>
    <h2>Welcome <%= locals.accountData.account_firstname %></h2>
<p>You're logged in</p>

<a class="add-link" href="/account/update/<%- accountData.account_id %>">Edit Account Information</a>

<% if (locals.accountData && (locals.accountData.account_type === "Admin" || locals.accountData.account_type === "Employee")) { %>
    <h3>Inventory Management</h3>
    <a class="add-link" href="/inv">Manage Inventory</a>
  <% } %>
